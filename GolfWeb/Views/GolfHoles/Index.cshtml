@model IEnumerable<GolfWeb.Models.GolfHole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GolfCourse.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoleNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Par)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Handicap)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GolfCourse.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoleNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Par)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Handicap)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GolfHoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GolfHoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GolfHoleID })
        </td>
    </tr>
}

</table>
